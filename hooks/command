#!/bin/bash

set -euo pipefail

DIR="$(cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd)"

. "$DIR/../lib/functions.bash" # must come before env.bash to support pull function
. "$DIR/../lib/env.bash"
. "$DIR/../commands/run.sh"